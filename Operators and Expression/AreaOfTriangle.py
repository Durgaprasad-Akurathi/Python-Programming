def AreaOfTraiangle(base, height):
    Area = (base * height) / 2
    return Area


base = int(input('Enter the base of the Triangle: '))
height = int(input('Enter the base of the Triangle: '))

print("Area of the Triangle is: ", AreaOfTraiangle(base, height))